{
  "name": "test-mail",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "preview": "npm run build && node index.js",
    "dev": "tsc && node index.js",
    "open": "npm run preview && xdg-open index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.13",
    "typescript": "^5.8.3"
  }
}
